services:
  - type: web
    name: pattern-space
    runtime: rust
    buildCommand: cargo build --release
    startCommand: ./target/release/pattern_space
    envVars:
      - key: ROCKET_ADDRESS
        value: 0.0.0.0
      - key: ROCKET_PORT
        value: 10000